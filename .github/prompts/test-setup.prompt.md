# Add integration test scenario

Add an integration test using this repository's existing test setup.

## Requirements
- Use existing fixtures, clients, and fakers.
- Validate status code and response body where relevant.
- Keep scenario-specific setup local to the test.
- Cover one happy path and one validation/auth error path.
- Keep runtime fast and deterministic.

## Inputs
- Target endpoint:
- Scenario:
- Expected status:
- Expected payload/validation error:
